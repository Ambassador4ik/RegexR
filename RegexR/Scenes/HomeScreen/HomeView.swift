import SwiftUI

struct HomeView: View {
    // MARK: States & Models
    
    // MARK: Body
    var body: some View {
        background
        ZStack {
            
        }
    }
    
    // MARK: Components
    private var background: some View {
        Color.backgroundColor.ignoresSafeArea(edges: .all)
    }
}
